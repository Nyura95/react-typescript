/**
 * Utils for a className with multiple class
 * @param {string[]} className
 * @version 1.0.0
 * @returns {string}
 */
export const joinClass = (...className: string[]): string => className.join(' ');
